<!DOCTYPE html>
<html lang="en">

<head>
  <title>Meme Details</title>
  <link rel="stylesheet" href="../css/bootstrap.min.css" />
  <link href="../stylesheets/navbar.css" rel="stylesheet">
  <link rel="stylesheet" href="../stylesheets/memeDetails/memes.css">
  <link rel="stylesheet" href="../stylesheets/memeDetails/navbar.css">
</head>

<body class="position-relative" data-bs-spy="scroll">
  <%- include('./partials/navbar.ejs') %>
  <!-- <section class="container">
    <h1><%= meme.name %></h1>
    <table class="table" id="memes-table">
      <thead>
        <tr>
          <th scope="col">ID</th>
          <th scope="col">Dimensions</th>
          <th scope="col">Image</th>
          <th scope="col">Name</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <th><%= meme.id %></th>
          <td><%= `${meme.width} x ${meme.height}` %></td>
          <td><img class="detailImage" src="<%= meme.url %>" alt="<%= meme.name %>" width="100" height="100"></td>
          <td><%= `${meme.name}`%></td>
        </tr>
      </tbody>
    </table>
    
  </section> -->

  <section class="container">

    <div class="row">
      <div class="col-4">
        <h1 class="text-success"><%= `${meme.name}`%></h1>
        <ul>
        <li>Dimensions: <%= `${meme.width} x ${meme.height}`%></li>
        <li>ID: <%= meme.id %> </li>
        <li>URL: <a href="<%=meme.url%>"> Click here for "<%= meme.name %>" source. </a></li>
        </ul>
      </div>
      <div class="col-8">
        <img class="w-50" src="<%= meme.url %>">
      </div>
    </div>
  
  </section>

    <%- include('./partials/scripts.ejs') %>
</body>

</html>